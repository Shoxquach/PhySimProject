# 图形学物理仿真 Lab4 大作业报告

## 题目：物理仿真驱动的小游戏 ——《愤怒的水鸟》（刚体 × 流体 Two-way 耦合）

---

## 一、项目概述

本项目基于课程 VCX 框架，实现了一个**以物理仿真为核心玩法**的三维弹弓小游戏。
玩家用弹弓发射"小鸟"或"水球"，击垮由木头 / 玻璃 / 石头搭成的结构、命中目标方块得分。
游戏把本学期三个 Lab 的成果整合并扩展：

| 课程内容 | 在本项目中的角色 |
| --- | --- |
| **Lab1 刚体**（冲量法 + FCL 碰撞 + 破碎） | 结构方块、发射体、碰撞与破坏 |
| **Lab2 FLIP 流体**（PIC/FLIP + 压力投影） | 水体（护城河、水坝蓄水、水球喷射） |
| **刚体 ↔ 流体耦合（Two-way）** | 物体推水产生水花、水流冲走物体、浮力 |

核心难点在于**刚体与流体的双向耦合**与**整体稳定性**，并以"关卡式、循序渐进"的方式逐步集成。

---

## 二、整体架构（模块化设计）

为了让多种物理系统能干净地组合，我们没有把所有逻辑堆进一个文件，而是采用
"独立子系统 → World 调度器 → Level 关卡"的三层结构：

```
4-Final/
├── World.h                     # 调度器：按固定顺序推进各子系统并插入耦合
├── Config.h                    # 全局参数（舞台缩放 WorldScale）
├── Physics/
│   ├── RigidWorld.h/.cpp       # 刚体子系统（原 Lab1，冲量法 + FCL + 破碎）
│   ├── FluidSolver.h           # FLIP/PIC 求解器（移植自 Lab2）
│   ├── FluidWorld.h            # 把单位立方体的流体映射到世界空间 + 浮力/边界查询
│   └── Coupling.h/.cpp         # ★ 所有刚体↔流体耦合逻辑集中于此
├── Levels/
│   ├── ILevel.h                # 关卡接口（Setup / Tick / Status / Shots）
│   ├── LevelCommon.h           # 材质调色板 + 胜负判定
│   ├── Level01..05_*.cpp       # 5 个关卡，各自独立成文件
│   └── LevelRegistry.cpp       # 关卡注册（新增关卡只需一行）
└── CaseAngryBirds3D.h/.cpp     # 渲染 + 输入 + 计分（物理逻辑委托给 World）
```

**设计要点**：
- 每个物理子系统**互不知道彼此**，只暴露查询/标记接口；
- `World` 用 `std::optional<FluidWorld>` 持有流体，**不用流体的关卡零开销**；
- 所有耦合代码集中在 `Coupling.cpp`，一旦不稳定只需排查这一处（对应评分中的"稳定性"）；
- 新增关卡 = 新增一个文件，**不改动已有代码**，便于分工协作。

---

## 三、关键方法与实现思路

### 1. 刚体（Lab1 扩展）
- 显式积分 + **冲量法**碰撞响应，四元数表示朝向；
- 用 **FCL** 做窄相碰撞检测，统一处理 球-球 / 球-盒 / 盒-盒；
- **破碎**：当接触冲量超过方块 `Toughness` 时，碎裂成 8 个碎片并赋予爆散速度；
- 木头 / 玻璃 / 石头通过密度与韧性区分（玻璃易碎、石头坚硬厚重）。

### 2. 流体（Lab2 移植）
- **FLIP/PIC 混合**：粒子携带速度，交错网格上做压力投影（Gauss–Seidel）；
- 保留了 Lab2 的有效插值（靠墙不被固体 0 速度污染）与防粒子重叠。

### 3. 刚体 ↔ 流体 双向耦合（本项目核心）
- **刚体 → 流体**：
  - `MarkRigidSolids`：每步把刚体占据的网格单元标记为**动态 solid**，并给该单元写入**刚体表面速度**作为移动边界条件 → 压力投影自然把水推开、挡住；
  - `PushParticlesOutOfRigid`：把钻进刚体内部的粒子推回表面并匹配法向速度 → 产生**水花**、避免穿透。
- **流体 → 刚体**：
  - **浮力**：按密度比 `ρ_water/ρ_body` 与浸没比例施加向上加速度（木头浮、石头沉）；
  - **流动拖拽**：采样物体处的流体速度，把物体拽向水流方向 → 决堤后水能**冲走**物体。
  - 关键修正：Two-way 把物体单元标成 solid 后，"格子是否为水"会失效，故浮力改用**逐列水面高度场（height field）**判断浸没，二者互不干扰。
- **水球发射体**：碰撞或入水时**爆裂**，向流体运行时**动态注入约 180 个粒子**（增加水量）。

### 4. 渲染与表现
- 天空**竖直渐变**（自定义 `sky` 着色器，全屏三角形）；
- 水粒子用**圆形点精灵**（`point` 着色器 `gl_PointCoord` 裁剪）+ **按速度着色**（蓝→青→黄→红）；
- 远山、云朵、树木等**装饰几何**（纯绘制，不参与物理）；
- 小鸟带眼睛与喙；双向光照（暖色主光 + 冷色补光）。

### 5. 游戏系统
- 弹弓：鼠标拖拽 → 释放，发射前显示**抛物线预览**；
- **自动装填**：发射体消失或静止 0.8s 后自动在弹弓上补充新发射体（可连发）；
- **计分 HUD**：屏幕顶部居中显示 `SCORE / 剩余目标 / 已用发数`（方块 +100、目标 +500、过关 +1000）；
- 5 个关卡 + 发射体（小鸟 / 水球）下拉选择 + 反弹/摩擦/子步等参数可实时调。

---

## 四、5 个关卡（循序渐进集成）

| 关卡 | 引入的新机制 | 说明 |
| --- | --- | --- |
| 1. Classic Tower | 纯刚体 | 双柱 + 横梁 + 玻璃 + 石顶，多目标 |
| 2. Stone Castle | 纯刚体 | 城墙 + 双塔 + 内堡，结构更复杂 |
| 3. Target Practice | 纯刚体 + 障碍 | 前后两排不同高度目标 + 玻璃挡墙 |
| 4. Moat（浮力） | 流体 → 刚体（单向浮力） | 木筏漂浮、石头下沉，木筏上有小要塞 |
| 5. Dam Break（双向） | 刚体 ↔ 流体（Two-way） | 石坝蓄水，击垮坝体后洪水冲垮右侧村庄 |

---

## 五、遇到的问题与解决方案

1. **Two-way 耦合后浮力失效**：物体单元被标成 solid，导致"采样格子是否为水"恒为否。
   → 改为根据**逐列水面高度场**判断浸没，浮力与 solid 标记解耦。
2. **发射前发射体掉落、水球"看不见"**：未发射的发射体也受重力下坠，导致弹弓上看不到。
   → 未发射时把发射体**固定（hover）在弹弓锚点**，发射后才释放。
3. **小鸟"一直往前飞"**：一是缺少自动装填（飞走后再不出现），二是重力偏弱、轨迹太平。
   → 增加重力使抛物线更明显 + 缩短寿命 + 自动装填。
4. **数值稳定性**：耦合容易发散。
   → 限制注入网格的 solid 速度（clamp）、浮力/拖拽系数随浸没比例与 dt 缩放并 clamp、
     流体每帧只步进一次而刚体用 6 子步、压力迭代 40 次。
5. **舞台太小且不易整体放大**：引入单一常量 `WorldScale`，对长度与重力同时缩放，
   得到"物理一致地放大"的同一场景（堆叠手感不变）。

---

## 六、编译与运行

```bash
# 进入项目目录后
xmake build lab4      # 编译
xmake run   lab4      # 运行（窗口程序）
```

操作：左侧选择关卡与发射体 → 按 `S` 进入发射模式 → 鼠标拖拽小鸟/水球并释放发射；
`R` 重置关卡，右键拖拽旋转相机。

提交前清理：

```bash
xmake clean -a        # 移除所有二进制与临时文件
```

---



## 七、Git 记录

代码全程使用 Git 管理，提交记录见 `lab4_log.txt`（由 `git log --stat` 导出）。
